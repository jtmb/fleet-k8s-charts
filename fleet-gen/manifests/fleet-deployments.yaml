---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: dev-hookie-deployment
  namespace: fleet-local
spec:
  repo: "https://github.com/jtmb/fleet-k8s-charts.git"
  branch: "master"
  paths:
    - "hookie"

---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata: 
  name: dev-media-stack-deployment
  namespace: fleet-local
spec:
  repo: "https://github.com/jtmb/fleet-k8s-charts.git"
  branch: "master"
  paths:
    - "media-stack"
    
---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: dev-website-stack-deployment
  namespace: fleet-local
spec:
  repo: "https://github.com/jtmb/fleet-k8s-charts.git"
  branch: "master"
  paths:
    - "website-stack"

---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: dev-wordpress-stack-deployment
  namespace: fleet-local
spec:
  repo: "https://github.com/jtmb/fleet-k8s-charts.git"
  branch: "master"
  paths:
    - "wordpress-stack/**"

---
apiVersion: fleet.cattle.io/v1alpha1
kind: GitRepo
metadata:
  name: dev-rancher-stack-deployment
  namespace: fleet-local
spec:
  repo: "https://github.com/jtmb/fleet-k8s-charts.git"
  branch: "master"
  paths:
    - "rancher-stack/**"


